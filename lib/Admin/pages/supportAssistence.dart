import 'package:flutter/material.dart';

class SupportAssistencePage extends StatelessWidget {
  //const DashboardPage({super.key});


  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child:Text("Support and assistence page"),
        ),
    );
  }
}